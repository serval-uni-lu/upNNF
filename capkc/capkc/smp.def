BootStrap: docker
From: debian:bookworm-slim

%files
    ./src /capkill/src
    ./test /capkill/test
    ./gen.cpp /capkill/gen.cpp
    ./dependencies /capkill/dependencies

%post
    echo "setup start"

    export DEBIAN_FRONTEND=noninteractive

    apt-get -y update
    apt-get -y install tzdata
    apt-get -y install g++ make zlib1g-dev libboost-dev libgmp-dev libgmpxx4ldbl ninja-build libboost-random-dev libboost-program-options-dev cmake

    cd /capkill/dependencies/networkit
    rm -rf build
    mkdir build
    cd build
    cmake -DCMAKE_BUILD_TYPE=Release -DCMAKE_POSITION_INDEPENDENT_CODE=ON ..
    make -j8

    cd /capkill
    g++ gen.cpp -o gen
    ./gen -DCONTAINER
    ninja build/smp

    chmod 755 /capkill/build/smp

%runscript
    /capkill/build/smp "$@"
